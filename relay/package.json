{
  "name": "easy-relay",
  "version": "2.0.0",
  "description": "Easy relay server — Socket.IO room-based relay for E2E encrypted messaging",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "socket.io": "^4.7.0"
  }
}
